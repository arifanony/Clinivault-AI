"""Evaluation: controlled offline retrieval benchmark.

Minimal, deliberately non-general measurement instrument for the CURRENT
baseline retrieval (hash embeddings, cosine, Top-K=5). Not an evaluation
framework; no network, no generation. See
docs/pipelines/retrieval-baseline-benchmark.md.
"""
