# Copy to `.env` in the repository root. Never commit `.env`.
# Generation (Gemini Developer API) reads this at request time.
# The observability UI can also accept a per-request key instead.
GOOGLE_API_KEY=
